@model List<bookChapterPageFrameContentObject>
@{
    Layout = null;
}
    @if (@Model != null)
    {
        if (@Model[0] != null)
        {
            foreach (bookChapterPageFrameContentObject Object in @Model)
            {

                <p>Object animation: @(Object.animation ?? "No animation")</p>
                <p>Object type: @(Object.type ?? "No type")</p>

            }
        }
    }
